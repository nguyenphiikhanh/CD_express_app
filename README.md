config script: "cp .env.example .env"<br/>
 <br/>
Change your config in .env file.<br/>
<br/>
Type script "npm install" to install packages.<br/>
<br/>
Type script "npm start" to start running app.<br/>
